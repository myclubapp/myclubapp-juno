import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-club-juno',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
